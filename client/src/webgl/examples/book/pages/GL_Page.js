export default class GL_Page {

    constructor(state, width, height) {
        this.state = state;
        this.texture = null;
        this.width = width;
        this.height = height;
    }

    update() {
    }

    dispose() {
    }

    onResize(e) {
    }
}
